import React from 'react';
import { Typography } from '@mui/material';

export const ChatPage: React.FC = () => {
  return <Typography variant="h5">Чат (в разработке)</Typography>;
};

export default ChatPage;
