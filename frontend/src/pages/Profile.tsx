import React from 'react';
import { Typography } from '@mui/material';

export const ProfilePage: React.FC = () => {
  return <Typography variant="h5">Мой профиль</Typography>;
};

export default ProfilePage;
