import React from 'react';
import { Typography } from '@mui/material';

export const AdminPage: React.FC = () => {
  return <Typography variant="h5">Админка (в разработке)</Typography>;
};

export default AdminPage;
